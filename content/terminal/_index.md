---
title: "Terminal"
type: terminal
# Dev-mode shell, not real content — keep it out of the sitemap so crawlers
# treat the GUI homepage (/) as the canonical entry point.
sitemap:
  disable: true
# Dev-mode terminal interface. Renders via layouts/terminal/{baseof,list}.html,
# fully isolated from the normal site chrome. Toggle lives in the top nav.
---
